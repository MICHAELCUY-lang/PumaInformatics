<?php

use App\Models\User;
use Spatie\Permission\Models\Role;

it('denies access if user does not have required role', function () {
    // Will be fully implemented after db is up and role seeders run.
    $this->assertTrue(true);
});
